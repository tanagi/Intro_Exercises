width = float(raw_input('What is the width of the room in meters? '))
length = float(raw_input('What is the length of the room in meters? '))
print 'Area of the room is %.2f square meters' % (width * length)
